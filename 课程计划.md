# 前端转 Agent 工程师 · 课程计划

面向已经会 TypeScript / 前端的人。模型（DeepSeek）是调用对象；你要学的是 **harness：循环、工具、评测、约束**。

仓库：[Superljf/jianfeng-Agent](https://github.com/Superljf/jianfeng-Agent)  
密钥只放各课 `.env`，不要提交、不要发到聊天。

## 怎么用这份计划

- 一课一个目录：`01-...`、`02-...`
- **上一课毕业，再开下一课**，不要在 01 里堆功能
- 每课结束看「毕业标准」，过了再往下
- 语言全程 TypeScript + Node 22

当前进度：**第一课内核已完成**，收尾是自己再跑 3 个真任务；然后开第二课。

---

## 总表

| 课 | 目录 | 状态 | 你要搞懂什么 | 大约 |
|---|---|---|---|---|
| 1 | [01-mini-agent](./01-mini-agent) | 进行中（内核已通） | 模型 → 工具 → 再问模型 | 1–2 天 |
| 2 | `02-eval` | 未开始 | 固定任务、自动判定成败 | 2–3 天 |
| 3 | `03-chat` | 未开始 | 同一会话多轮接着改 | 2 天 |
| 4 | `04-approval` | 未开始 | 写文件/跑命令前先问人 | 1–2 天 |
| 5 | `05-frontend-task` | 未开始 | 改真实小 Vue/React 组件 | 3–5 天 |
| 6 | `06-context` | 未开始 | AGENTS.md、会话日志、压缩直觉 | 2–3 天 |
| 7 | `07-trace-ui` | 未开始 | 用前端把每一步工具画出来 | 3–4 天 |
| 8 | `08-own-tool` | 未开始 | 自己加一个领域工具并评测 | 2–3 天 |

合计大约 **3–4 周**（每天 1–2 小时）到作品集能拿得出手。不是报班节奏，是练习节奏。

---

## 第一课 · 最小循环

**目录：** `01-mini-agent`  
**目标：** 自己写通一条 coding agent，而不是调框架。

**已有：**

- `read` / `edit` / `bash`
- 工作区锁在 `playground/`
- `--fake` 与真 DeepSeek
- `write` 映射成 `edit`，去掉多余的 `playground/` 前缀

**步骤：**

1. 先读 [怎么读tools.md](./怎么读tools.md)（用你上次的终端对照）  
2. 读 `src/loop.ts` 的 `for` 循环，能讲出每一步发生什么  
3. `pnpm start -- --fake "写一个 hello.ts"`  
3. 配 `.env` 后跑真模型  
4. 自己再跑 3 个任务（改内容、用 bash 列文件、故意越界）  
5. 对照终端：工具名、参数、结果、最终总结  

**毕业：** 能不看文档解释「为什么会套一层 playground/」「未知工具为什么没把进程打死」。

---

## 第二课 · 评测

**目录：** `02-eval`  
**目标：** 知道 Agent 稳不稳定，而不是「感觉它行」。

**步骤：**

1. 写 5 条固定任务（例如：创建文件、改一处字符串、跑 `node hello.ts` 输出符合预期）  
2. 每条有「开始前的文件快照」和「成功条件」  
3. 跑完自动打勾/打叉，输出一份 `results.md`  
4. 同一套任务连跑 3 次，记通过率  

**毕业：** 有一张表：任务 × 成功/失败，而不是截一张终端当证明。

**为什么在聊天和 UI 前面：** 没有评测，后面加的功能都不知道有没有把正确率打崩。

---

## 第三课 · 多轮对话

**目录：** `03-chat`  
**目标：** 一次进程里连续说多句，上下文接着用。

**步骤：**

1. 命令行循环读 stdin，空行或 `/exit` 结束  
2. 消息数组跨轮保留  
3. `/new` 清空会话  
4. 用第一课的工具，不要换模型层  

**毕业：** 「先写 hello.ts，再改成输出名字」可以在同一进程里完成，不用重启。

---

## 第四课 · 审批

**目录：** `04-approval`  
**目标：** 危险操作先问人。这是 Cursor / dsh 权限弹窗的缩微版。

**步骤：**

1. `edit` 与 `bash` 执行前在终端打印参数，等 `y/n`  
2. `read` 默认放行  
3. 拒绝时把「用户拒绝」当工具结果返回给模型，循环继续  
4. 加一条评测：拒绝写入时磁盘不变  

**毕业：** 能讲清「审批是 harness 的事，不是模型自觉」。

---

## 第五课 · 真实前端任务

**目录：** `05-frontend-task`  
**目标：** Agent 改你熟悉的 UI，而不是 `hello.ts`。

**步骤：**

1. 在 `playground/` 放一个最小 Vue 或 React 组件（按钮、颜色、文案即可）  
2. 任务写成自然语言：「把按钮改成主色，并补一个禁用态」  
3. 成功条件：文件里出现约定 class / 文本；能 `npm test` 或至少能 `build`  
4. 接入第二课的评测，至少 5 条前端任务  

**毕业：** 有一个「前端仓库上的 Agent 任务集」，这是作品集主件。

---

## 第六课 · 上下文

**目录：** `06-context`  
**目标：** 控制模型看见什么。

**步骤：**

1. 启动时加载 `playground/AGENTS.md`  
2. 把每次 tool 结果截断到长度上限，避免把终端刷爆进上下文  
3. 可选：步数多了之后做一次「摘要再继续」（先手写规则，不调复杂框架）  
4. 对比：有/无 AGENTS.md 时，同一前端任务的通过率  

**毕业：** 能说出「模型可见 ⟺ 你塞进 messages 的东西」。

---

## 第七课 · 过程可视化

**目录：** `07-trace-ui`  
**目标：** 用前端把 Agent 干活的过程画出来（发挥你的本职）。

**步骤：**

1. 本地一个极简页：任务输入、步骤列表、最终回答  
2. 每一步展示：工具名、参数、截断后的结果  
3. 后端仍是第三课的循环，前端只消费事件  
4. 不要做成 ChatGPT 皮肤；重点是 **trace**，不是气泡  

**毕业：** 给别人演示时，能指着某一步说「这里模型调了 edit」。

---

## 第八课 · 自己的工具

**目录：** `08-own-tool`  
**目标：** 领域能力来自你加的工具，不是再堆 prompt。

**建议选题（挑一个）：**

- `read_component`：按组件名列出 props / 文件路径  
- `lint_css`：对某个文件跑 stylelint 或自定义规则  
- `figma_token`：读一份本地 design token JSON 并回答「主色是什么」  

**步骤：**

1. 定义 schema + execute  
2. 系统提示只告诉模型有这个工具  
3. 加 3 条评测，证明不用这个工具会失败或更慢  

**毕业：** README 里写清：工具契约、一个成功例子、通过率。

---

## 不要做的事

- 第一课还没收尾就上 LangChain / 多智能体  
- 先做漂亮 UI 再做评测  
- 在 01 目录里无限加功能  
- 把密钥提交到 GitHub  

---

## 作品集（全部课结束后）

别人只需要看这三样：

1. `01-mini-agent`：你能讲明白的最小循环  
2. `02-eval` + `05-frontend-task`：有通过率的前端任务集  
3. `07-trace-ui` 或 `08-own-tool`：要么能看见过程，要么有一个你自己的工具  

到这里，你不是「会调 API 的前端」，而是能把 Agent 接到真实前端工作流上的人。
